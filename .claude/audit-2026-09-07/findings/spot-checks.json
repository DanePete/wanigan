{
  "sessions-01": ["confirmed", "Sessions.tsx:768 calls sessions.forget directly; the file imports no ConfirmNote."],
  "fleet-01": ["confirmed", "Fleet.tsx:510 renders Note tone=error (role alert by default) with dur(Date.now() - since) inside it, re-rendered by the 1s clock."],
  "fleet-02": ["confirmed", "index.ts:1605 returns true unconditionally; sessions.ts:2136 killSession is void and returns silently when the session is missing or exited."],
  "control-01": ["confirmed", "reconcileRunningNodes is called only from index.ts:1016 at startup; no other caller in src/main."],
  "control-02": ["confirmed", "control.ts:685 runs the gate at node.worktree ?? project.path; control.ts:552 launches every node with isolate:true; worktrees.ts:342 cuts from baseBranch ?? head with no ref argument."],
  "control-08": ["confirmed", "control.ts:714 stores 0 when costStatus is not 'reported'; Control.tsx:550 renders usd(totalCostUsd)."],
  "batches-01": ["confirmed", "No .scroll-x overflow rule in index.css or any surface sheet; only compact.css:79 sets touch scrolling. Batches.tsx uses the class four times."],
  "batches-02": ["confirmed", "batch/index.ts:157 lists FROM runs r with no kind predicate; headless.ts inserts runs with kind 'headless'."],
  "batches-03": ["confirmed", "index.ts:1652-1662 catches refreshModels errors and returns {models:[], source:'unavailable — …'}, so the renderer's catch never fires."],
  "batches-04": ["confirmed", "grep finds no caller of runVariant or judgePair outside src/main/batch/evals.ts, and no evals:variant or evals:judge channel."],
  "insights-01": ["confirmed", "preload/index.ts:281 calls call('spend:sync') without days; main index.ts:1942 accepts days."],
  "insights-02": ["plausible", "Follows from insights-01 by construction; the subtraction at Insights.tsx:181 was read by the auditor, not re-run by me."],
  "insights-03": ["plausible", "spend.ts sums cost_usd with no unpriced count; the contrast with usage.ts costStatus is documented in the source comments the auditor quotes."],
  "insights-27": ["confirmed", "unifiedSpend, spendBySurface and effortDistribution have no callers outside spend.ts; only otel.ts comments mention them."],
  "plugins-01": ["confirmed", "plugins.ts:533 returns {plugins: [], note} on a failed CLI call and Plugins.tsx:148 stores that empty array as an answered catalog. A second independent audit reported the same defect."],
  "plugins-02": ["confirmed", "context/config.ts:494-538 reads enabledPlugins from settings layers; plugins.ts never does. A second independent audit reported the same defect."],
  "git-02": ["confirmed", "git.ts:450, 500 and 518 each return [] when the git call fails; the renderer prints 'No commits yet', an empty branch list and 'No stashes'."],
  "git-18": ["confirmed", "Git.tsx:318 runs loadPr on mount and on every branch change; gh.ts runs gh pr list, a network call, under a lead that says Wanigan only reads until a button is pressed."],
  "usage-02": ["confirmed", "accounts.ts seed() inserts a 'Personal' row whenever the harness supports accounts and the table is empty, with no check that the CLI exists; claude-limits.ts:264 and limits.ts:121 call list() for both harnesses on every snapshot."],
  "usage-03": ["confirmed", "claude-limits.ts:208 and :212 resolve null on timeout and spawn error; :240 maps a missing identity to state 'signed-out' with the /login instruction."],
  "runs-01": ["confirmed", "App.tsx:456 calls loadShell on every window focus and :286 calls setProviders unconditionally; HeadlessRuns.tsx:141-153 keys its reset effect on provider.launchFields, a fresh array each time."],
  "context-01": ["confirmed", "Context.tsx never reads .harness (grep count 0); instructions.ts:125 and :1271 declare the chain as Claude Code's loader only."],
  "context-02": ["confirmed", "Context.tsx:529 sends '/init' through skills.send; skills.ts:670 refuses any invoke that is not in the project's skill catalogue, and /init is a CLI built-in."],
  "scout-01": ["confirmed", "improvement-scout.css:103 sets flex-direction: column on every direct label child of .scout-setting-grid with higher specificity than .scout-toggle's row rule."],
  "scout-02": ["confirmed", "ImprovementScout.tsx:497 derives the status options from the current rows and nothing ever resets the selected status when its option disappears."]
}
